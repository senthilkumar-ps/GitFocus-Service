## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)

spring.datasource.url=jdbc:postgresql://13.234.254.213:5432/postgres
spring.datasource.username=Daguser
spring.datasource.password=Dag*123

#spring.datasource.url=jdbc:postgresql://localhost:5432/GitFocus
#spring.datasource.username=postgres
#spring.datasource.password=root


# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = none

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.show-sql=false

server.port=8888

spring.profiles.active=@activatedProperties@

#keycloak.enabled=false
#keycloak.auth-server-url=http://localhost:8080/auth
#keycloak.realm=DAG
#keycloak.resource=GitFocus
#keycloak.public-client=true
#keycloak.ssl-required=external
#keycloak.cors=true
#keycloak.enable-basic-auth=true
#keycloak.bearer-only=true
#keycloak.securityConstraints[0].authRoles[0]=User
#keycloak.securityConstraints[0].securityCollections[0].name=protected
#keycloak.securityConstraints[0].securityCollections[0].patterns[0]=/*